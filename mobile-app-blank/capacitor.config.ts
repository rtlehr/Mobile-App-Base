import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rosslehr.mobileappblank',
  appName: 'mobile-app-blank',
  webDir: 'www'
};

export default config;
